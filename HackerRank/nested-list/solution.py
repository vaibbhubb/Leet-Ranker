if __name__ == '__main__':    students = []    n = int(input())    for _ in range(n):        name = input()        score = float(input())        students.append([name, score])    # Get all grades    grades = []    for student in students:        grades.append(student[1])    # Remove duplicates and sort    grades = sorted(set(grades))    # Second lowest grade    second_lowest = grades[1]    # Collect names with second lowest grade    names = []    for student in students:        if student[1] == second_lowest:            names.append(student[0])    # Sort names alphabetically    names.sort()    # Print each name    for name in names:        print(name)